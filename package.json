{
  "name": "windcloud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js --ignore-pattern 'games/**'",
    "format": "prettier --write .",
    "prepare": "husky install",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "prettier": "^2.8.8",
    "husky": "^8.0.0",
    "lint-staged": "^13.1.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write",
      "git add"
    ]
  }
}
